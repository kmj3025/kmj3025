#include<iostream>
using namespace std;


int arr[1000];
int main()
{
	int n; //정수의 개수
	int v; //찾고자 하는 수
	int p = 0; // 개수
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> arr[i];
	}
	cin >> v;
	for (int k = 0; k < n; k++)
	{
		if (v == arr[k])
		{
			p++;
		}
	}
	cout << p;

	return 0;
}
