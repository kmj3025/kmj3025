#include<iostream>
using namespace std;

int main()
{
	int h, m;
	cin >> h >> m;
	int sum = h * 60 + m;
	sum = sum - 45;
	if (sum < 0)
	{
		sum = 24 * 60 + sum;
	}
	cout << sum / 60 << " " << sum % 60;





	return 0;
	
}
